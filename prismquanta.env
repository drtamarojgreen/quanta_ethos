# PrismQuanta Environment Variables
# This file defines all shared paths and configurations for the ecosystem.
# All scripts must source this file.

# Root directory of the PrismQuanta project
PRISMQUANTA_ROOT=$(pwd)

# Directories for file-based queues
QUEUE_DIR=${PRISMQUANTA_ROOT}/queue
QUEUE_PENDING=${QUEUE_DIR}/pending
QUEUE_IN_PROGRESS=${QUEUE_DIR}/in_progress
QUEUE_COMPLETED=${QUEUE_DIR}/completed
QUEUE_FAILED=${QUEUE_DIR}/failed
QUEUE_HUMAN_APPROVAL=${QUEUE_DIR}/human_approval

# Directories for actions
ACTIONS_DIR=${PRISMQUANTA_ROOT}/actions
ACTIONS_PENDING=${ACTIONS_DIR}/pending
ACTIONS_IN_PROGRESS=${ACTIONS_DIR}/in_progress
ACTIONS_FAILED=${ACTIONS_DIR}/failed

# Log directory
LOG_DIR=${PRISMQUANTA_ROOT}/logs

# Bin directory for executables
BIN_DIR=${PRISMQUANTA_ROOT}/bin
